title: discord-alert-transport
description: Portable Discord webhook transport — Python package + multi-language reference.
remote_theme: pages-themes/minimal@v0.2.0
plugins:
  - jekyll-remote-theme
  - jekyll-relative-links
  - jekyll-seo-tag
relative_links:
  enabled: true
  collections: true
include:
  - CHANGELOG.md
defaults:
  - scope:
      path: ""
    values:
      layout: default
